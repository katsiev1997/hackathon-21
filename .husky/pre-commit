if git diff --cached --name-only | grep -q '^react-router-ssr/'; then
  echo "Запускаем lint-staged…"
  (cd react-router-ssr && npx lint-staged)
  echo "lint-staged завершён."
  echo "Проверяем сборку…"
  (cd react-router-ssr && npm run build)
  echo "Сборка прошла."
fi
